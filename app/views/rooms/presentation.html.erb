<%= turbo_stream_from "room_#{@room.id}_presentation" %>

<% content_for :title, "Apresentação · #{@room.passcode} · StageManager" %>
<% content_for :body_class, "presentation-display-page" %>

<%= render "rooms/presentation_display", room: @room %>

<%= render "rooms/audio_player", room: @room %>
